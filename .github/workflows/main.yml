name: Gerar APK Sensi VIP
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Baixando seu codigo
        uses: actions/checkout@v4 # Atualizado para v4

      - name: Configurando Ambiente
        uses: actions/setup-java@v4 # Atualizado para v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Transformando HTML em APK
        run: |
          npm install -g @bubblewrap/cli
          # Criando um arquivo de configuração padrão para o Bubblewrap não perguntar nada
          mkdir -p ~/.bubblewrap
          echo '{"jdkPath":"/usr/lib/jvm/java-17-openjdk-amd64","androidSdkPath":"/home/runner/android-sdk"}' > ~/.bubblewrap/config.json
          
          # Build direto sem interação
          bubblewrap build --noprompt

      - name: Disponibilizar APK para Download
        uses: actions/upload-artifact@v4 # ESSA É A LINHA QUE ESTAVA DANDO ERRO
        with:
          name: Instalador-Sensi-VIP
          path: ./*.apk